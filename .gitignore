# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config.
/.bundle

# Ignore all environment files.
/.env*

# Ignore all logfiles and tempfiles.
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

# Ignore master key for decrypting credentials and more.
/config/master.key

# environment files
.env*
!.env.example
!.env.vault

# aws credentials
.aws/

# Ignore dotenv files
.env
.env.*
!.env.example
!.env.vault

# Ignore dotenv vault keys
.env.vault.keys
.env.me

.flaskenv*
!.env.project

# Add coverage directory
/coverage

# pulumi
infrastructure/.pulumi/history
infrastructure/.pulumi/backups
infrastructure/.pulumi/stacks/*.json
!infrastructure/.pulumi/stacks/Pulumi.*.yaml

# Ignore database dumps and backups
*.dump
*.sql
*.bak
*.backup
/db/backups/

# Local Pulumi state - contains sensitive data
infrastructure/.pulumi/

# AWS credentials and profiles
.aws/
aws.json
aws-credentials.json
credentials.yml

# Rails credentials and key files
config/credentials.yml.enc
config/master.key
/config/credentials/*.key

# Ignore node modules
/node_modules/

# Ignore coverage reports
/coverage/

# Ignore temporary editor files
*.swp
*.swo
*~
.DS_Store
.vscode/
.idea/

# Ignore compiled assets
/public/assets
/public/packs
/public/packs-test

# Ignore yarn files
/yarn-error.log
yarn-debug.log*
.yarn-integrity

# Ignore docker volumes
docker-volume/

# Ignore local development files
/development/

# Ignore private keys, certificates, and auth tokens
*.pem
*.key
*.cert
*.token
*.crt
*.p12
*.pfx
auth_tokens.yml

# Ignore any local passwords or secret files
*password*
*secret*
*_secret*
*secrets*
*api_key*
*apikey*